Install rubocop

Learn to use vi mode for ultra hacks

Have a ruby-tricks class that creates an Invoker with default params 

Invoker will handle running the program normally
It will have an InputManager with normal params

When testing, in each test:
  create an Invoker with a specific param (a file to invoke)
  give the Invoker an InputManager with the correct user input if necessary
  testing 